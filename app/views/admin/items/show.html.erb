<h2>商品詳細</h2>
<div>
  <%= attachment_image_tag @item, :image %>
  <p>商品名：<%= @item.name %></p>
  <p>商品説明：<%= @item.introduction %></p>
  <p>ジャンル：<%= @item.genre.name %></p>
  <p>税込価格（税抜価格）：<%= @item.price %></p>
  <p>販売ステータス：<%= @item.is_active_i18n %></p>
  <p><%= link_to "編集する", edit_admin_item_path(@item) %></p>
</div>

